import React from 'react'

// mui components
import Box from '@mui/material/Box'
import { styled } from '@mui/material/styles'

const Root = styled(Box)((aa) => {
	console.log(aa)
	return {}
})

export default Root
