// Material Dashboard 2 React base styles
import colors from '@theme/base/colors'

const { text } = colors

const formLabel = {
	styleOverrides: {
		root: {
			color: text.main,
		},
	},
}

export default formLabel
